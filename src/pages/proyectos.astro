---
import Layout from "../layouts/Layout.astro";
import { SITE_CONFIG } from "../config/site";
---

<Layout title="项目" description="与大模型应用与云原生工程相关的代表性项目">
  <section class="max-w-4xl mx-auto px-4 py-14">
    <h1 class="text-4xl font-bold mb-4">项目</h1>
    <p class="text-gray-700 dark:text-gray-300">
      这些项目集中体现了我在 RAG、Agent 工程化与云原生存储方向的工作重心。
    </p>

    <div class="mt-8 space-y-6">
      <article class="p-6 rounded-xl border border-gray-200 dark:border-gray-800">
        <h2 class="text-xl font-semibold">企业智能知识库系统（RAG应用）</h2>
        <p class="mt-2 text-gray-700 dark:text-gray-300">
          围绕企业内部文档、技术手册与故障案例构建统一检索问答平台，采用层级切片与重叠切片策略，结合混合检索与重排提升召回质量，提供流式问答接口并形成反馈闭环。
        </p>
        <div class="mt-3 flex flex-wrap gap-2 text-xs">
          {["RAGFlow", "Elasticsearch", "向量检索", "混合召回", "重排", "FastAPI"].map((t) => (
            <span class="px-2 py-1 rounded bg-gray-100 dark:bg-gray-800">{t}</span>
          ))}
        </div>
      </article>

      <article class="p-6 rounded-xl border border-gray-200 dark:border-gray-800">
        <h2 class="text-xl font-semibold">智能问答助手（面向客服团队）</h2>
        <p class="mt-2 text-gray-700 dark:text-gray-300">
          面向客户支持团队的多轮问答能力建设，支持上下文记忆、追问澄清与知识溯源，提供可集成的 API 以便与内部工作台对接。
        </p>
        <div class="mt-3 flex flex-wrap gap-2 text-xs">
          {["多轮对话", "上下文管理", "溯源", "服务化接口"].map((t) => (
            <span class="px-2 py-1 rounded bg-gray-100 dark:bg-gray-800">{t}</span>
          ))}
        </div>
      </article>

      <article class="p-6 rounded-xl border border-gray-200 dark:border-gray-800">
        <h2 class="text-xl font-semibold">AI Agent 原型（工具调用与流程自动化）</h2>
        <p class="mt-2 text-gray-700 dark:text-gray-300">
          搭建可扩展的 Agent 框架，围绕内部 API 进行工具封装与函数调用编排，实现一句话触发的自动化流程，并支持对话式交互与多步任务执行。
        </p>
        <div class="mt-3 flex flex-wrap gap-2 text-xs">
          {["LangChain", "Dify", "Function Calling", "工具编排"].map((t) => (
            <span class="px-2 py-1 rounded bg-gray-100 dark:bg-gray-800">{t}</span>
          ))}
        </div>
      </article>

      <article class="p-6 rounded-xl border border-gray-200 dark:border-gray-800">
        <h2 class="text-xl font-semibold">Kubernetes CSI 插件与 OpenStack Cinder 驱动</h2>
        <p class="mt-2 text-gray-700 dark:text-gray-300">
          负责存储系统与云平台的集成，覆盖动态卷供给、快照、克隆等能力，并通过兼容性测试与社区认证，提升容器化环境的资源利用率与稳定性。
        </p>
        <div class="mt-3 flex flex-wrap gap-2 text-xs">
          {["Kubernetes", "CSI", "OpenStack", "Cinder", "容器化部署"].map((t) => (
            <span class="px-2 py-1 rounded bg-gray-100 dark:bg-gray-800">{t}</span>
          ))}
        </div>
      </article>
    </div>

    <div class="mt-10 p-6 rounded-xl bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-800">
      <p class="text-gray-700 dark:text-gray-300">
        如需进一步了解项目细节或讨论合作，欢迎通过 <a class="text-purple-600 dark:text-purple-400 hover:underline" href={`mailto:${SITE_CONFIG.email}`}>{SITE_CONFIG.email}</a> 联系。
      </p>
    </div>
  </section>
</Layout>
